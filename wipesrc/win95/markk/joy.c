/* Joystick Handling Functions for LIBETC, LIBAPI PAD functions.
	This function is called from within the VBL routine. */
#include	"libetc.h"

void	PollJoysticks(void)
{
 	if(Joystick1_Switch = _DEVICE_ON)
	{
		/* Access Joystick Status. */

		/* Transfer Data to Buffer. */
	}

 	if(Joystick2_Switch = _DEVICE_ON)
	{
		/* Access Joystick Status. */

		/* Transfer Data to Buffer. */
	}
}
