/* Mouse Handling Functions for LIBETC, LIBAPI PAD functions.
	This function is called from within the VBL routine. */
#include	"libetc.h"

void	PollMouse(void)
{
	if(Mouse1_Switch = _DEVICE_ON)
	{
		/* Access Mouse Status. */

		/* Transfer Data to Buffer. */
	}
}

